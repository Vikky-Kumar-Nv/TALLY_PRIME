# Force install all optional dependencies regardless of platform
optional=false

# Skip platform checks for esbuild
target_platform=linux
target_arch=x64

# Allow installation of packages for different platforms
force=true

# Disable platform checks
ignore-platform=true
