# Force clean install
prefer-offline=false

# Skip platform checks for esbuild (using package.json overrides instead)
# target_platform=linux  # DEPRECATED - handled in package.json
# target_arch=x64        # DEPRECATED - handled in package.json

# Disable platform checks
# ignore-platform=true   # DEPRECATED

# Registry settings
registry=https://registry.npmjs.org/

# Cache and performance
prefer-online=true
fund=false
audit=false
